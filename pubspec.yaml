name: dna_gg
description: "DNA layer: gg CLI workflow guides, repo setup scripts and CI check"
version: 0.5.1
repository: https://github.com/ggdna/dna_gg.git

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  dna_index: ^0.1.2
  dna_install: ^0.1.2

dev_dependencies:
  helix: ^1.9.0
  lints: ^6.1.0
  test: ^1.32.0